# Tapo Monitor

Dedicated Android/Android TV viewer for a local Tapo C520WS RTSP stream. Defaults to camera IP 192.168.0.13, prompts once for the Tapo Camera Account credentials, keeps the screen awake, runs full-screen, and automatically reconnects if the RTSP stream drops.
